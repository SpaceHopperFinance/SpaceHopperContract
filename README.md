# Space Hopper Contract

The smart contracts for Space Hopper [https://spacehopper.finance].

# Local Development

The following assumes the use of `node@>=10`.

## Install Dependencies

`npm install`

## Compile Contracts

`npx hardhat compile`
